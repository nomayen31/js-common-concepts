function sum(a, b, c) {
   for (let i = 0; i < sum.length; i++) {
    const element = sum[i];
    
   }
    const element = a + b + c;
    return element;
}

const total = sum(45,50,12,45,98,56)
console.log(total);